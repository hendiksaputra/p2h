# Memory

## 2026-05-06

- Proyek greenfield `p2h`: Next.js 16 + Prisma 6 + MySQL. Migrasi awal ada di `prisma/migrations/20260506120000_init/`.
- Server actions yang mengembalikan objek error tidak kompatibel dengan tipe `form action` di React 19; error formulir dialihkan lewat `redirect` + query `error`.
- Jika simpan kendaraan gagal setelah menambah field `unit_no`: cek kolom `unit_no` di tabel `vehicles` dan status migrasi. Error Prisma **P3009** berarti migrasi `20260506140000_add_vehicle_unit_no` pernah gagal — pakai `migrate resolve` (rolled-back atau applied) lalu `migrate deploy` atau ALTER manual; pesan user kini memakai `src/lib/prisma-errors.ts`.
- Tabel `users` ditambahkan lewat Prisma (`prisma/migrations/20260506150000_add_users_table/`) dengan kolom utama: `username`, `password`, `nik`, `fullname`, `position` (plus `id` sebagai primary key).
- Modul users berada di **Pengaturan**: `/settings/users` (sub-nav di layout `src/app/settings/`). URL lama `/users` mengalihkan ke sana. Menu navbar utama: **Pengaturan** → `/settings`.
- **RBAC:** Tabel `roles`, `permissions`, `role_permissions`, `user_permissions`, kolom `users.role_id`. Template izin per role di `/settings/roles`. Di modal **Role & izin** (`/settings/users`): dropdown role + **checkbox** untuk `create.vehicles`, `ubah.vehicles`, `read.vehicles` — tersimpan di `user_permissions` lewat `updateUserRolePermissionsAction` (`src/app/actions/rbac.ts`). Ganti role mengisi ulang checkbox dari template role. Urutan kunci: `src/lib/vehicle-permissions.ts`. Seed (`npm run db:seed`) mengisi permission + role contoh. Migrasi `20260506170000_add_user_permissions`. Setelah ubah `schema.prisma`, hentikan `npm run dev` bila perlu lalu `npx prisma migrate deploy` + `npx prisma generate`.
- **Migrasi setengah jalan / P3009 / “tabel sudah ada”:** Jika `migrate deploy` gagal karena migrasi tercatat *failed* padahal perubahan SQL sudah ada di MySQL (mis. kolom `unit_no` atau tabel `users` sudah dibuat), tandai migrasi itu dengan `npx prisma migrate resolve --applied NAMA_FOLDER_MIGRASI`, lalu `migrate deploy` lagi. Jangan memakai `--applied` jika tabel/kolom memang belum ada.
- Simpan user: `src/app/actions/users.ts`, validasi `src/lib/user-validation.ts`, password `bcrypt` (`hash(password, 10)`). Update role/izin role: `src/app/actions/rbac.ts`.
- **Login:** `/login` (middleware melindungi seluruh app kecuali halaman login & aset statis). Sesi JWT (HS256) di cookie httpOnly `p2h_session` — `jose`, `src/lib/auth-jwt.ts`, `src/lib/auth-session.ts`, `src/middleware.ts`. `AUTH_SECRET` di `.env` (production wajib). `loginAction` / `logoutAction`: `src/app/actions/auth-login.ts`. Seed membuat user `admin` (password `SEED_ADMIN_PASSWORD` atau default `Admin1234!`) setelah RBAC.
- **Enforcement permission kendaraan:** Tombol/route/action sekarang ikut izin efektif user. Sumber izin efektif: jika user punya `user_permissions` untuk modul kendaraan maka itu **override role**, jika kosong baru fallback ke role. Implementasi: `src/lib/vehicle-access.ts`; dipakai di `src/app/vehicles/page.tsx`, `src/app/vehicles/new/page.tsx`, `src/app/vehicles/[id]/edit/page.tsx`, dan `src/app/actions/vehicles.ts`.
- Modal `Role & izin` di `/settings/users` sekarang menampilkan dua grup checkbox: **Izin kendaraan** + **Izin pengaturan** (`read.settings`, `manage.users`, `manage.roles`). Server action `updateUserRolePermissionsAction` menerima keduanya (whitelist: `VEHICLE_PERMISSION_KEYS` + `SETTINGS_PERMISSION_KEYS`).
